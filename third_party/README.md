# Third-party dependencies

This project expects the CRL reference implementation as a git submodule.

To add it:
```bash
git submodule add https://github.com/wang-kevin3290/scaling-crl.git third_party/scaling-crl
git submodule update --init --recursive
```
